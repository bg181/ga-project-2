<!DOCTYPE html>
<html>
<head>
  <title>KooKApp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <nav>
      <ul>
        <li><%= link_to "Kook", root_path %></li>
        <li><%= link_to "Browse Spaces", restaurants_path %></li>
        <% if user_signed_in? %>
          <% else %>
        <% end %>
      </ul>
    </nav>
  </header>


<% flash.each do |name, message| %>
  <div class="flash-message flash-message-<%= name %>">
    <%= message %>
  </div>
<% end %>


<a href="">Sign Up</a> 

//If logged in just add a property - if not sign up//

<%= yield %>

</body>
</html>
